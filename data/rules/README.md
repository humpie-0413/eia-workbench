# data/rules/

규칙 데이터 (JSON / YAML) 보관.

용도 예:
- 육상풍력 환경영향평가 대상 여부 체크 기준 (용량 MW, 입지 등)
- 평가서 목차 점검 체크리스트
- 의견 대응표 카테고리 분류 룰

실제 파일은 `writing-plans` 단계에서 기능별로 결정한다.
민감 규칙(내부 사례집 등)은 여기에 두지 말고 `data/samples/private/` 또는 별도 환경변수로.
