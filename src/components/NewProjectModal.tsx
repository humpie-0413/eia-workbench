export default function NewProjectModal() {
  return (
    <button type="button" disabled aria-disabled="true"
      className="h-9 px-3 rounded-md border border-border bg-surface text-small text-text-tertiary cursor-not-allowed">
      새 프로젝트
    </button>
  );
}
