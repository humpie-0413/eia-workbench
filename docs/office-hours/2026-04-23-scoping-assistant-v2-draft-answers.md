# OH v2 답변 초안 (Claude 생성, 사용자 검토·수정용)

**Source:** `docs/office-hours/2026-04-23-scoping-assistant-v2-redesign.md`
**Draft date:** 2026-04-23
**Principle:** CLAUDE.md §2-3 (법적 단정 금지) / §2-4 (원문 재호스팅 금지) / v0 MVP "작게 시작" / 솔로 파일럿 (외부 평가사 인터뷰 불가) / spec v1 T1 실패 교훈 (미검증 가정이 구현까지 흐르면 재앙)
**Author:** Claude (초안만, 최종 판단은 사용자)

---

## Q1 — MVP v2 대상 업종

**질문 원문:** MVP v2 대상 업종

**선택지 요약:**
- (a) onshore_wind 만 유지 (v1 과 동일 범위, rule 만 확장)
- (b) onshore_wind + onshore_solar (육상 태양광)
- (c) 기타: 해상풍력 / 연료전지 / …

**Claude 추천:** (a)

**이유:**
- v1 T1 이 실패한 직후이므로 범위를 넓히면 "미검증 스키마 × 업종 수" 로 감사 부담이 곱으로 증가.
- solo 파일럿 + 외부 평가사 인터뷰 불가 상황에서 태양광·해상풍력의 **업종별 특수 규칙** (해상풍력 = 해역이용협의, 태양광 = 농지·산지 특례) 을 Claude 추론만으로 메우면 또 한 번 재앙.
- "작게 시작 → 법령 대조가 PASS 한 단일 업종부터 사용자에게 선보이기" 가 v0 MVP 정신과 일치.

**만약 (b) 를 선택하면:** UI form 2종 + E2E 2세트 + rule pack 2개 + 법령 대조 2배. 공수 ~ 1.8x. solar 업종은 "농지·산지 특례" 가 별표3/4 외 별도 법령 (신에너지 및 재생에너지 개발·이용·보급 촉진법) 을 추가로 참조해야 할 가능성 → 스코프 팽창.

**만약 (c) 를 선택하면:** 해상풍력은 환경영향평가법 외에 해양환경관리법·공유수면관리법 등이 겹치며 법령 대조 난이도 급상승. 연료전지는 도시가스 관련 법령까지. **MVP 범위 밖 — 비권장.**

**사용자 확정 (아래 체크):**
- [x] (a) 추천대로 onshore_wind 유지
- [ ] (b) onshore_wind + onshore_solar
- [ ] (c) 다른 업종: ___________

**사용자 코멘트:** ___________

---

## Q2 — spec v2 rule 개수 목표

**질문 원문:** spec v2 에서 rule 개수 목표

**선택지 요약:**
- (a) 5개 (최소 교정 — EIA 대상, 소규모 EIA 2개, 산지전용, 발전용량 경계)
- (b) 6–8개 (용도지역 5개 × 면적 조합 일부 + 발전용량)
- (c) 10개 이상 (별표3·별표4 전체 테이블을 거의 그대로 반영)

**Claude 추천:** (a)

**이유 (OH 원문에서 (b) → (a) 로 수정 제안):**
- OH 원문은 (b) 추천이었으나 T1 실패 직후라 **"확장하면 또 못 맞힐 위험"** 이 큼. 5개면 "EIA 대상 / 보전관리 / 계획관리 / 농림·자연환경보전 / 산지전용" 정도로 커버 가능.
- rule 하나 = 법령 원문 대조 1건 = findings 문서 1줄. 5 → 8 로 늘리면 감사 시간 거의 2배.
- 솔로 파일럿 피드백 받기 전에는 "적게 맞게 시작 > 많이 애매하게 시작".

**만약 (b) 를 선택하면:** 용도지역 × 면적 조합이 2~3 세트 추가. 커버리지는 좋아지지만 첫 PDF 대조에서 틀린 숫자가 섞일 확률도 올라감. 감사 리스크 + 테스트 공수 +40%.

**만약 (c) 를 선택하면:** 별표 전체 반영 = 유지보수 부담 폭발. 법령 개정마다 rule pack 전면 수정. **v1 의 교훈을 반대로 학습한 선택 — 비권장.**

**사용자 확정:**
- [x] (a) 5개로 시작 (Claude 수정 추천)
- [ ] (b) 6~8개 (OH 원문 추천)
- [ ] (c) 10개 이상
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q3 — 면적 기준 단위 통일

**질문 원문:** "면적 기준" 단위 통일

**선택지 요약:**
- (a) 제곱미터 (㎡) — 법령 원문과 일치
- (b) 헥타르 (ha) — 업계 관용 표기
- (c) 둘 다 입력 받아 내부 정규화 (ha → ㎡ 변환)

**Claude 추천:** (c)

**이유:**
- 법령은 "660㎡" 와 "1만㎡ (1 ha)" 가 섞여 있어 **사용자가 입력 단위를 선택** 할 수 있으면 실수 확률 감소.
- 내부 저장은 항상 ㎡ 로 정규화 → rule DSL 도 ㎡ 단일. DB·rule pack 의 경량 유지.
- 입력 폼은 "숫자 input + 단위 selector (㎡/ha)" 두 필드로 간단. UX 부담 작음.

**만약 (a) 를 선택하면:** 사용자가 ha 숫자를 ㎡ 로 일일이 환산 → "1 ha 인 줄 알았는데 10000 찍음" 이면 rule 미발동. 실무 현장 경험상 단위 혼동은 흔한 사고.

**만약 (b) 를 선택하면:** 법령 "660㎡" 를 "0.066 ha" 로 사용자에게 요구 → 반올림 오류 + 소수점 자리 사고. 산지전용 660㎡ 경계 판정에서 실패 확률↑.

**사용자 확정:**
- [ ] (a) ㎡ 만
- [ ] (b) ha 만
- [x] (c) 둘 다 + 내부 ㎡ 정규화 (추천)
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q4 — 추가 필드 확정 (입력 스키마)

**질문 원문:** 추가 필드 확정

**선택지 요약:**
- (a) 필수만: `site_area_m2` (number), `land_use_zone` (enum)
- (b) (a) + `nearby_protected_area_within_km`
- (c) (b) + `elevation_change_m`

**Claude 추천:** (a)

**이유:**
- MVP 는 **법령 직접 수치** 로만 판단. (b)(c) 는 "가파른 경사는 토양영향↑" 같은 정성 판단 → DSL 로 수치화하는 순간 또 미검증 가정.
- 보호구역 인접성·고저차는 실제 EIA 보고서 단계에서 다뤄지는 영역 (현지조사·항공사진 분석 필요). 스코핑 단계에서는 오버.
- "법령 수치만" 원칙 유지 → 감사 대상 축소 → T1 재감사 PASS 확률 증가.

**만약 (b) 를 선택하면:** `nearby_protected_area_within_km` 의 "보호구역" 정의 (국립공원/습지/생태자연도 등급/문화재 등) 가 다층이라 enum 정의만으로도 별도 조사. 감사 부담 + 입력 피로도.

**만약 (c) 를 선택하면:** 고저차 정보는 사용자가 측정 없이는 입력 불가 → 대부분 빈값 → 빈값 처리 로직 부담만 증가.

**사용자 확정:**
- [x] (a) 필수만 (추천)
- [ ] (b) + 보호구역 인접성
- [ ] (c) + 고저차
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q5 — `land_use_zone` enum 항목

**질문 원문:** `land_use_zone` enum 항목

**선택지 요약:**
- (a) 5개 관리지역: `conservation_management` / `production_management` / `planning_management` / `agricultural_forestry` / `natural_environment_conservation`
- (b) 6개 (a + `urban`)
- (c) 3개 간소화

**Claude 추천:** (a)

**이유:**
- 별표4 가 정확히 이 5개 축으로 나눠져 있어 **rule pack과 enum이 1:1 매핑**. 번역 손실 없음.
- 육상풍력은 도시지역 설치가 드물어 `urban` 은 YAGNI. 필요해지면 v3 에서 추가.
- (c) 간소화는 법령 명칭과 괴리 → 사용자(환경영향평가대행사) 관점에서 "왜 법령 용어를 안 쓰나" 로 불신 유발.

**만약 (b) 를 선택하면:** `urban` rule 은 실제 케이스가 거의 없어 테스트만 늘어남. 또 도시지역은 용도지역 3차 분류 (주거/상업/공업/녹지) 가 따로 있어 단일 `urban` 으로 묶기 애매.

**만약 (c) 를 선택하면:** enum 단순화 vs rule pack 정확도 트레이드오프에서 rule 정확도가 희생됨. **법령 축과 UI 축이 일치하지 않으면 v2 재설계 의미가 반감됨.**

**사용자 확정:**
- [x] (a) 5개 관리지역 (추천)
- [ ] (b) 6개 (+urban)
- [ ] (c) 3개 간소화
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q6 — `capacity_mw` 필수 여부

**질문 원문:** `capacity_mw` 필수 여부

**선택지 요약:**
- (a) 필수 — v1 과 동일
- (b) 선택 — 사업면적만 있어도 소규모 EIA 판단 가능 (MW 없으면 MW rule 은 `onUndefined: skip`)
- (c) 3-옵션 라디오: "설정 안 함 / 1 MW 미만 / 정확한 수치"

**Claude 추천:** (b)

**이유:**
- 실무상 영향평가대행사 초기 검토 단계에서는 **발전용량이 미확정 (타당성조사 중)** 이 흔함. MW 강제하면 초기 검토 자체 불가.
- spec v1 에서 도입한 `onUndefined: skip` 시맨틱이 이미 설계돼 있음 → 재사용.
- 면적·용도지역 축만으로도 "소규모 EIA 대상" 은 판단 가능 → 가장 흔한 사용 케이스 커버.

**만약 (a) 를 선택하면:** 초기 검토 단계 사용자가 "임의 숫자" 를 입력해 rule 발동 → 결과 신뢰도 저하. v1 과 동일한 UX 결함.

**만약 (c) 를 선택하면:** 3-옵션 라디오는 UX 로 매력적이지만 "1 MW 미만" 이 무엇을 의미하는지 rule DSL 에서 어떻게 해석할지 모호 (0.5 MW 로 고정? 아니면 범위 비교?). 구현 복잡도↑.

**사용자 확정:**
- [ ] (a) 필수 (v1 유지)
- [x] (b) 선택 + onUndefined: skip (추천)
- [ ] (c) 3-옵션 라디오
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q7 — 결과 개수 변동 대응

**질문 원문:** v1 은 4 rule → 항상 4개 카드. v2 는 조건부 실행 (용도지역 특정일 때만 발동) 섞임.

**선택지 요약:**
- (a) 발동 안 된 rule 은 숨김
- (b) 발동 안 된 rule 도 "해당 없음" 회색 표시
- (c) 발동 rule / skip rule 을 섹션 분리 (accordion 으로 skip 섹션 접음)

**Claude 추천:** (c)

**이유:**
- 환경영향평가 실무에서는 **"왜 이 rule 은 skip 됐는지"** 감사 추적이 중요. (a) 숨기면 나중에 "왜 이 검토가 빠졌느냐" 추적 불가.
- accordion 접기로 기본 상태는 (a) 처럼 간결, 펼치면 (b) 처럼 완전한 상태 → 두 장점 결합.
- 솔로 파일럿 단계에서 "왜 이 결과가 나왔나" 를 본인이 검증할 때 유용.

**만약 (a) 를 선택하면:** UI 간결하지만 투명성 희생. 사용자가 "내가 입력한 용도지역이 올바른가" 검증 불가.

**만약 (b) 를 선택하면:** 5 rule 중 2~3 개가 항상 "해당 없음" 이면 UI 가 회색 카드로 지저분해짐. 중요도 낮은 rule 이 시각적 노이즈.

**사용자 확정:**
- [ ] (a) skip rule 숨김
- [ ] (b) 회색 "해당 없음"
- [x] (c) 섹션 분리 + accordion (추천)
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q8 — 근거 citation 링크 처리

**질문 원문:** 근거 citation 링크 처리

**선택지 요약:**
- (a) text only, 링크 없음 (v1)
- (b) citation text + `citation_url` 필드 추가, UI 에서 외부링크 열림
- (c) (b) + 첫 클릭 시 "법령 해석 주의 안내" 모달

**Claude 추천:** (b)

**이유:**
- 법제처 링크는 공개 URL → 재호스팅 금지 원칙 (§2-4) 에 저촉 안 됨. `citation_url` 필드는 YAML rule pack 에 1줄 추가.
- 사용자가 "별표3 원문 어디?" 클릭 1회로 이동 가능 → 실무 편의 큼.
- (c) 의 안내 모달은 배너/하단 주의문에서 이미 커버되므로 중복 UX.

**만약 (a) 를 선택하면:** 사용자가 citation 읽고 확인하려면 수동으로 law.go.kr 검색 → 마찰. 실무 사용성 저하.

**만약 (c) 를 선택하면:** 첫 클릭 모달은 초보 사용자에게 유용하지만, 환경영향평가대행사 대상 SaaS 에서는 **전문가에게 잔소리** 로 느껴질 위험. UX 피로.

**사용자 확정:**
- [ ] (a) text only
- [x] (b) `citation_url` 추가 + 외부링크 (추천)
- [ ] (c) (b) + 주의 모달
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q9 — v1 산출물 처리

**질문 원문:** 현재 WIP commit `1cc39a8` 에 포함된 v1 산출물 처리
- `docs/superpowers/specs/2026-04-22-scoping-assistant-design.md`
- `docs/plans/feature-scoping-assistant.md`

**선택지 요약:**
- (a) v1 산출물 그대로 둠, v2 는 새 파일
- (b) v1 을 rename 해 보존 (`…-v1-superseded-by-v2` suffix), v2 가 canonical
- (c) v1 덮어쓰기

**Claude 추천:** (b)

**이유:**
- findings 에서 "v1 의 어디가 틀렸다" 를 참조할 때 **v1 파일이 원형으로 남아있어야 감사 이력 완결**. rename 은 삭제 아님 → git log 에서 추적 가능.
- 두 파일이 같은 폴더에 공존하면 "어느 게 최신?" 혼선 → `-superseded-by-v2` suffix 가 명시적 경고.
- 미래에 v3 가 나오면 v2 가 또 superseded 될 수 있어 패턴 정립에 유용.

**만약 (a) 를 선택하면:** `…-2026-04-22-…` 와 `…-2026-04-23-…` 가 혼재. 한 주 뒤 보면 둘 다 "scoping-assistant" 라 혼선. 누군가 v1 을 우연히 참조할 위험.

**만약 (c) 를 선택하면:** git diff 로는 변경 이력 남지만 "v1 과 v2 를 나란히 비교" 가 불가 (rename 추적 vs 덮어쓰기 diff 는 가독성 차이). **감사 친화적이지 않음.**

**사용자 확정:**
- [ ] (a) v1 보존 + v2 새 파일
- [x] (b) v1 rename `-superseded-by-v2` (추천)
- [ ] (c) 덮어쓰기
- [ ] 커스텀: ___________

**사용자 코멘트:** ___________

---

## Q10 — v2 commit 전략

**질문 원문 + 현재 상태 업데이트:**
OH 원문 작성 시점에는 findings 도 untracked 였으나, **현재는 `1cc39a8` 에 findings + v1 + 기타 모두 포함 상태**. 따라서 Q10 의 선택지 (a)(b)(c) 를 현재 상황에 맞게 재해석해야 함.

**현재 기준 선택지 (재해석):**
- (a) v2 spec + v2 plan + v1 rename 을 **한 개 커밋** ("refactor: rule pack v2 after legal audit")
- (b) 두 단계 커밋: ① v1 rename + v2 spec, ② v2 plan
- (c) 기능 브랜치 (`feature/scoping-assistant-v2`) 신설 후 작업

**Claude 추천:** (b)

**이유:**
- 각 커밋이 독립적으로 revert 가능 — v2 spec 만 받아들이고 plan 은 재작성 상황도 감당.
- v1 rename 은 spec 작성과 분리하는 것이 일반적이지만, 같은 commit 에 묶어도 diff 가독성 해치지 않음 (단일 rename + 단일 add).
- (c) 브랜치 생성은 main 에 이미 WIP 커밋이 있어 "브랜치는 어디서 갈라지나" 결정 필요 → 오버헤드. 기존 흐름 (main 직접) 유지가 단순.

**만약 (a) 를 선택하면:** 하나의 큰 커밋으로 "v2 전환 단일 이벤트" 가 됨. revert 시 스펙·플랜 동시 소멸. 다음 세션이 구현까지 간다면 implementation 커밋까지 묶을 가능성 → 커밋 과대.

**만약 (c) 를 선택하면:** 브랜치는 v0 project-shell 처럼 PR flow 가 필요할 때 유용. scoping-assistant 구현 단계 (T1 재감사 PASS 이후) 에서는 고려할 만하나, **스펙·플랜 단계에서는 과도**.

**사용자 확정:**
- [ ] (a) 단일 커밋 (refactor)
- [ ] (b) 두 단계 커밋 (추천)
- [ ] (c) feature 브랜치 신설
- [x] 커스텀: T1 재감사 결과 / v1 rename + spec v2 / plan v2 의 3단계 분리 커밋

**사용자 코멘트:** ___________

---

## 종합 기본값 (Claude 추천 스택)

모든 Q 에 Claude 추천을 그대로 수락할 경우:

- Q1=(a) — onshore_wind 만
- Q2=(a) — 5 rule (OH 원문 (b) 에서 수정)
- Q3=(c) — ㎡/ha 이중 입력 + 내부 ㎡
- Q4=(a) — 필수 필드만 (`site_area_m2` + `land_use_zone`)
- Q5=(a) — 5개 관리지역
- Q6=(b) — `capacity_mw` 선택 + onUndefined: skip
- Q7=(c) — 발동/skip 섹션 분리 + accordion
- Q8=(b) — `citation_url` 외부링크
- Q9=(b) — v1 rename `-superseded-by-v2`
- Q10=(b) — 두 단계 커밋

**일괄 수락 시 회신 예시:** "Q2 만 (a) 로 변경, 나머지 추천 수락" 또는 "전체 추천 수락"

---

## 불확실 항목 (?)

없음 — 위 10개는 Claude 의 설계 판단으로 답변 가능한 수준. 단, **법령 관련 수치** (예: Q2 의 "5 rule" 이 실제 별표3/4 에 대응하는 구조인지) 는 T1 재감사 이후 확정. 본 답변 초안은 **구조·절차 결정** 만 다루며 수치는 별개.

---

## 사용자 action

1. 본 파일 열어 각 Q 체크박스 선택 + 필요 시 코멘트
2. 저장 (`NOTES-LOCAL.md` 와 달리 이 파일은 git 추적 대상이므로 수정 내용이 diff 로 노출됨 — 민감 정보 작성 주의)
3. 저장 후 사용자 판단:
   - 답변 완료 시점에 commit 여부 결정 (`docs: OH v2 answers` 등)
   - 또는 PDF 수령 완료까지 보류 후 일괄 commit
